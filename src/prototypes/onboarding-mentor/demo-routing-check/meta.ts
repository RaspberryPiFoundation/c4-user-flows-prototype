import type { PrototypeMeta } from '../../types'

// SCAFFOLDING — safe to delete once real prototypes exist. It only proves the
// registry discovers folders and the routing works.
export const meta: PrototypeMeta = {
  title: 'Routing check (demo)',
  owner: 'Divya',
  hypothesis:
    'Dropping a folder in src/prototypes is enough to get a card on the landing page and a working URL, with no shared file edited.',
  status: 'sketch',
}
